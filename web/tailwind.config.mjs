// Configuración de Tailwind CSS
//
// Este proyecto usa Tailwind CSS v4, donde la configuración principal vive
// en CSS (ver src/styles/global.css):
//   - El modo oscuro por clase se define ahí con @custom-variant.
//   - Los tokens de tema (colores, fuentes) se definen ahí con @theme.
//
// Este archivo se mantiene para configuraciones que solo se pueden expresar
// en JavaScript (plugins legacy, presets compartidos). Si lo necesitas,
// cárgalo desde global.css con: @config "../../tailwind.config.mjs";

/** @type {import('tailwindcss').Config} */
export default {};
